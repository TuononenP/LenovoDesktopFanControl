<Window x:Class="LenovoDesktopFanControl.Views.Controls.ColorPickerWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:loc="clr-namespace:LenovoDesktopFanControl.Views.Markup"
        Title="{loc:Loc ColorPickerTitle}"
        Width="430" Height="440" MinWidth="390" MinHeight="420"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        UseLayoutRounding="True"
        SnapsToDevicePixels="True"
        Background="{StaticResource WindowBrush}"
        Foreground="{StaticResource TextPrimaryBrush}"
        FontFamily="{StaticResource AppFont}"
        AutomationProperties.Name="{loc:Loc ColorPickerTitle}">
    <Grid Margin="24,22,24,20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Text="{loc:Loc ColorPickerTitle}"
                   FontSize="21"
                   FontWeight="SemiBold" />

        <Border Grid.Row="1" Margin="0,16,0,18" Padding="14"
                Background="{StaticResource SurfaceBrush}"
                BorderBrush="{StaticResource BorderBrush}"
                BorderThickness="1" CornerRadius="12">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="14" />
                    <ColumnDefinition Width="120" />
                </Grid.ColumnDefinitions>
                <Border Background="{Binding PreviewBrush}" CornerRadius="9" />
                <StackPanel Grid.Column="2" VerticalAlignment="Center">
                    <TextBlock Text="{loc:Loc ColorPickerPreview}"
                               FontSize="10.5" Foreground="{StaticResource TextMutedBrush}" />
                    <TextBlock Text="{Binding HexValue}"
                               Margin="0,4,0,0" FontSize="16" FontWeight="SemiBold" />
                </StackPanel>
            </Grid>
        </Border>

        <Grid Grid.Row="2">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="28" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="54" />
            </Grid.ColumnDefinitions>

            <TextBlock Text="R" VerticalAlignment="Center" FontWeight="SemiBold" Foreground="#FFFF5964" />
            <Slider Grid.Column="1" Minimum="0" Maximum="255" TickFrequency="1"
                    Style="{StaticResource HorizontalSliderStyle}"
                    Value="{Binding Red, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
            <TextBox Grid.Column="2" Margin="10,0,0,0" Padding="6,3" TextAlignment="Center"
                     Text="{Binding Red, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />

            <TextBlock Grid.Row="1" Text="G" VerticalAlignment="Center" FontWeight="SemiBold" Foreground="#FF37DC7D" />
            <Slider Grid.Row="1" Grid.Column="1" Minimum="0" Maximum="255" TickFrequency="1"
                    Style="{StaticResource HorizontalSliderStyle}"
                    Value="{Binding Green, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
            <TextBox Grid.Row="1" Grid.Column="2" Margin="10,0,0,0" Padding="6,3" TextAlignment="Center"
                     Text="{Binding Green, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />

            <TextBlock Grid.Row="2" Text="B" VerticalAlignment="Center" FontWeight="SemiBold" Foreground="#FF5B9DFF" />
            <Slider Grid.Row="2" Grid.Column="1" Minimum="0" Maximum="255" TickFrequency="1"
                    Style="{StaticResource HorizontalSliderStyle}"
                    Value="{Binding Blue, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
            <TextBox Grid.Row="2" Grid.Column="2" Margin="10,0,0,0" Padding="6,3" TextAlignment="Center"
                     Text="{Binding Blue, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />

            <TextBlock Grid.Row="3" Grid.ColumnSpan="3" Margin="0,14,0,0"
                       Text="{loc:Loc ColorPickerHex}" FontSize="10.5"
                       Foreground="{StaticResource TextMutedBrush}" />
            <TextBox Grid.Row="3" Grid.Column="1" Grid.ColumnSpan="2" Margin="0,31,0,0" Padding="8,5"
                     Text="{Binding HexValue, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
        </Grid>

        <StackPanel Grid.Row="3" Margin="0,20,0,0" HorizontalAlignment="Right" Orientation="Horizontal">
            <Button MinWidth="90" Padding="12,7" Content="{loc:Loc Cancel}" IsCancel="True" />
            <Button MinWidth="90" Margin="8,0,0,0" Padding="12,7" Content="{loc:Loc Apply}"
                    IsDefault="True" Click="Apply_Click" />
        </StackPanel>
    </Grid>
</Window>
